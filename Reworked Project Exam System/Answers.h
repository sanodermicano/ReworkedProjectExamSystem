#pragma once
#include "main.h" //?

class Answers 
{
	/*		//keep or remove or change later
public:
	Answers();
	Answers(bool,char);
	void setAll(bool,char=' ');
	bool getTF();
	char getChoice();
private:
	bool TF_;
	char choice_;
	*/
};